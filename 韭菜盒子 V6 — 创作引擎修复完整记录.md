# 韭菜盒子 V6 — 创作引擎修复完整记录

## 最终状态 ✅

| 功能 | 状态 | 验证 |
|------|------|------|
| 文生图 (gpt-image-2) | ✅ 正常 | 生产环境验证 |
| 图生图 (gpt-image-2) | ✅ 正常 | multipart `/v1/images/edits` |
| 并发生成 | ✅ 正常 | 按钮不再禁用，红色角标显示任务数 |
| Material Symbols 图标 | ✅ 正常 | 本地 woff2 (3.5MB) |
| 字体乱码 | ✅ 已根治 | 无 CDN 依赖 |

---

## 关键文件索引

| 文件 | 职责 |
|------|------|
| [media-generation.ts](file:///Users/by3/Documents/jiucaihezi-v6/src/api/media-generation.ts) | **核心 API 层** — 所有生成请求的单一入口 |
| [useCreationEngine.ts](file:///Users/by3/Documents/jiucaihezi-v6/src/composables/useCreationEngine.ts) | **引擎层** — 并发管理、文件检测、图片压缩 |
| [useCreation.ts](file:///Users/by3/Documents/jiucaihezi-v6/src/composables/useCreation.ts) | **状态层** — cpState 响应式状态管理 |
| [CreationPanel.vue](file:///Users/by3/Documents/jiucaihezi-v6/src/components/creation/CreationPanel.vue) | **UI 层** — 创作面板组件 |
| [creationModels.ts](file:///Users/by3/Documents/jiucaihezi-v6/src/data/creationModels.ts) | **模型配置** — 所有创作模型定义 |
| [base.css](file:///Users/by3/Documents/jiucaihezi-v6/src/styles/base.css) | **字体配置** — 本地 @font-face 声明 |

---

## 生产环境路由表（源码验证）

```
┌─────────────────┬──────────────────────────────┬─────────────────────────────────┐
│ 模型            │ 提交                          │ 轮询                             │
├─────────────────┼──────────────────────────────┼─────────────────────────────────┤
│ gpt-image-2 文生│ POST /v1/images/generations   │ 同步（无需轮询）                  │
│ gpt-image-2 图生│ POST /v1/images/edits (multi) │ 同步                             │
│ grok / veo      │ POST /v1/video/generations     │ GET /v1/video/generations/:id    │
│ seedance        │ POST /v1/videos                │ GET /v1/videos/:id               │
│ suno            │ POST /suno/submit/music         │ GET /suno/fetch/:id              │
└─────────────────┴──────────────────────────────┴─────────────────────────────────┘
```

---

## NewAPI 渠道状态（服务器 47.82.86.196）

> [!IMPORTANT]
> 数据库连接：`docker exec jiucaihezi-new-api-postgres psql -U newapi -d 'new-api'`

### 图片渠道

| ID | 名称 | 上游 | status | 备注 |
|----|------|------|--------|------|
| 16 | ZZ 生图 | ai.t8star.cn | 1 ✅ | 文生图/图生图均可 |
| 30 | z生图 | zxai.work | 1 ✅ | 文生图可用 |
| 28 | gpt-image-2 | — | **3 🔴** | T8 key 耗尽，手动禁用 |
| 34 | 我的GPT-IMAGE-2 | — | **3 🔴** | T8 key 耗尽 + 413 过大，手动禁用 |
| 19 | 超便宜 gpt 生图 | — | 2 🔴 | 已禁用 |
| 23 | SBapi | — | 2 🔴 | 已禁用 |

### 视频渠道

| ID | 名称 | status |
|----|------|--------|
| 16 | ZZ 生图 (含 veo/seedance/grok) | 1 ✅ |
| 18 | GROK-zhenzhen | 1 ✅ |
| 33 | seedace2.0 | 1 ✅ |

> [!WARNING]
> NewAPI 有自动恢复渠道功能。`status=2` 会被自动恢复，**必须用 `status=3`（手动禁用）才能永久禁止**。

---

## 本次修复的问题根因

### 1. 429 "no available image quota"
- **不是**用户额度问题
- **是** T8 上游渠道 Key 余额耗尽
- NewAPI 随机选渠道，选到坏的就 429
- **解决**：禁用 channel #28, #34 (status=3)

### 2. 图生图不生效
- `useCreationEngine.ts` 检查 `snap.task === 'image-image'`，但上传图片不会自动切换 task
- **解决**：改为智能检测 `snap.files.length > 0`

### 3. 图生图 413 / CORS 524
- JSON body 带 base64 数据太大 → Cloudflare 524 超时 / T8 413 拒绝
- **解决**：改用 multipart `/v1/images/edits` + Canvas 压缩（max 2048px, JPEG 0.9→0.5）

### 4. 字体乱码
- `fonts.loli.net` CDN 不稳定，Material Symbols 字体加载失败
- **解决**：Material Symbols woff2 本地打包 (3.5MB)，零 CDN 依赖

### 5. 无法并发生成
- 按钮 `:disabled="cpState.generating"` 布尔锁
- **解决**：移除 disabled，使用 `runningTasks` 计数器 + 红色角标

---

## Nginx 配置备注

- `client_max_body_size`: **200m**（已从 50m 升级，支持图片上传）
- `proxy_read_timeout`: 600s
- `proxy_send_timeout`: 600s
- 配置文件：`/etc/nginx/sites-enabled/api.jiucaihezi.studio.conf`

---

## 架构决策记录

1. **图生图用 multipart 而非 JSON** — NewAPI 的 `ConvertImageRequest` 对 `RelayModeImagesEdits` 强制要求 multipart。JSON body 里的 image 字段只在 `/v1/images/generations` 时由 ImageRequest DTO 透传，但会被 Cloudflare 524 超时。
2. **图片压缩在前端做** — Canvas API 压缩到 4MB 以内，比修改后端限制更可靠。
3. **字体本地化** — 彻底消除对 `fonts.loli.net` / `fonts.googleapis.com` 的依赖，生产环境零外部字体请求。
4. **渠道 status=3** — 防止 NewAPI 自动恢复功能（`ShouldEnableChannel`）把坏渠道恢复。
