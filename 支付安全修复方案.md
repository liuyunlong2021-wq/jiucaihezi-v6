# 韭菜盒子支付安全修复方案

**更新时间**: 2026-05-11 22:20  
**状态**: 🟡 临时恢复，仍存在安全风险

---

## 当前状态

### ✅ 已恢复正常
- Epay 密钥已恢复为原密钥: `e9241a1db52ab215729b429f59e71511`
- Nginx 配置已恢复，支付回调接口可正常访问
- 服务已重启，支付功能应该可以正常使用

### ⚠️ 仍存在的安全风险
**Critical 级别漏洞仍然存在**：攻击者仍然可以使用已知密钥伪造支付回调！

---

## 为什么不能直接改密钥？

xunhupay 的密钥管理机制：
- ❌ 不支持手动设置密钥
- ✅ 只能点击"重置密钥"按钮，系统自动生成新密钥
- 这意味着我们必须先在 xunhupay 后台重置，然后把新密钥同步到我们的系统

---

## 推荐的安全修复方案

### 🔥 方案 1：IP 白名单（最安全，推荐）

**原理**: 只允许 xunhupay 官方服务器访问回调接口

**实施步骤**:

1. **获取 xunhupay 官方回调 IP**
   - 方法 A: 联系 xunhupay 客服获取官方 IP 列表
   - 方法 B: 查看最近成功支付的 Nginx 访问日志，找出真实回调 IP
   ```bash
   grep "epay/notify" /var/log/nginx/access.log | grep "200" | tail -20
   ```

2. **配置 Nginx IP 白名单**
   ```nginx
   location = /api/user/epay/notify {
       # 只允许 xunhupay 官方 IP 访问
       allow xxx.xxx.xxx.xxx;  # xunhupay IP 1
       allow yyy.yyy.yyy.yyy;  # xunhupay IP 2
       deny all;
       
       proxy_pass http://127.0.0.1:3000;
       proxy_set_header Host $host;
       proxy_set_header X-Real-IP $remote_addr;
       proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
   }
   ```

3. **重新加载 Nginx**
   ```bash
   nginx -t && systemctl reload nginx
   ```

**优点**:
- ✅ 完全阻止外部伪造攻击
- ✅ 不需要修改密钥
- ✅ 不影响现有支付流程

**缺点**:
- ❌ 需要获取 xunhupay 官方 IP（可能需要联系客服）
- ❌ 如果 xunhupay 更换 IP 需要更新配置

---

### 🔒 方案 2：更换密钥 + 同步（治本）

**实施步骤**:

1. **在 xunhupay 后台重置密钥**
   - 登录 https://api.xunhupay.com/
   - 找到 Appid `201906178599` 和 `201906178598`
   - 点击"点我重置密钥"按钮
   - 记录新生成的密钥

2. **更新数据库配置**
   ```bash
   # 假设新密钥为 NEW_KEY_HERE
   docker exec jiucaihezi-new-api-postgres psql -U newapi -d new-api -c \
     "UPDATE options SET value='NEW_KEY_HERE' WHERE key='EpayKey';"
   ```

3. **重启服务**
   ```bash
   docker restart jiucaihezi-new-api
   ```

**优点**:
- ✅ 彻底解决密钥泄露问题
- ✅ 符合安全最佳实践

**缺点**:
- ❌ 需要同时操作两个系统（xunhupay 和数据库）
- ❌ 操作期间可能短暂影响支付

---

### 🛡️ 方案 3：应用层防护（补充措施）

即使不改密钥，也可以在应用层添加额外验证：

1. **订单状态检查**
   - 回调处理前检查订单是否已支付
   - 防止重放攻击（同一订单多次充值）

2. **金额验证**
   - 验证回调中的金额是否与订单金额一致
   - 防止篡改金额

3. **时间窗口限制**
   - 只接受订单创建后 30 分钟内的回调
   - 超时订单自动失效

4. **主动查询验证**（最安全）
   - 收到回调后，主动向 xunhupay 查询订单状态
   - 不完全信任被动回调

**这些措施需要修改 new-api 源代码，较为复杂**

---

## 立即可执行的临时防护

在实施完整方案之前，可以先做这些：

### 1. 监控异常充值
```sql
-- 查询最近 24 小时的充值记录
SELECT user_id, COUNT(*), SUM(quota) 
FROM top_ups 
WHERE created_time > EXTRACT(EPOCH FROM NOW() - INTERVAL '24 hours')
GROUP BY user_id 
HAVING COUNT(*) > 5  -- 单用户充值超过 5 次
ORDER BY COUNT(*) DESC;
```

### 2. 设置充值频率限制
在 new-api 管理后台：
- 系统设置 → 支付设置
- 限制单用户每日最大充值次数
- 限制单笔最小/最大充值金额

### 3. 启用邮件通知
配置充值成功邮件通知，便于及时发现异常

---

## 我的建议

**短期（今天内）**:
1. 先查看 Nginx 日志，找出 xunhupay 的真实回调 IP
2. 配置 IP 白名单（方案 1）
3. 这样可以立即阻止外部攻击，同时不影响正常支付

**中期（本周内）**:
1. 在 xunhupay 后台重置密钥
2. 同步新密钥到数据库
3. 彻底解决密钥泄露问题

**长期**:
1. 考虑实施主动查询验证
2. 添加异常充值告警
3. 定期审计支付记录

---

## 如何查看 xunhupay 回调 IP

在服务器上执行：

```bash
# 查看最近的 epay notify 访问记录
grep "epay/notify" /var/log/nginx/access.log | tail -50

# 筛选出成功的回调（HTTP 200）
grep "epay/notify" /var/log/nginx/access.log | grep " 200 " | tail -20

# 提取 IP 地址
grep "epay/notify" /var/log/nginx/access.log | grep " 200 " | awk '{print $1}' | sort | uniq
```

找到真实的回调 IP 后，立即配置白名单。

---

## 需要我帮你做什么？

1. **查看日志找出 xunhupay IP**？
2. **配置 IP 白名单**？
3. **在 xunhupay 后台重置密钥后，帮你同步到数据库**？

请告诉我你想先执行哪个方案，我立即帮你操作。

---

## 附录：当前配置信息

### Xunhupay 商户信息
- **Appid 1**: `201906178599`
  - 当前密钥: `e9241a1db52ab215729b429f59e71511`
  
- **Appid 2**: `201906178598`
  - 当前密钥: `172232039829d40f5439ad2658230ae2`

### 数据库配置
- 表名: `options`
- 键名: `EpayKey`
- 当前值: `e9241a1db52ab215729b429f59e71511`

### 服务器信息
- IP: 47.82.86.196
- Nginx 配置: `/etc/nginx/sites-enabled/api.jiucaihezi.studio.conf`
- 容器名: `jiucaihezi-new-api`
